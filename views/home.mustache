<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <title>Todo List</title>
  <link rel="stylesheet" href="/css/main.css">
</head>

<body style="text-align:center;">
    <h2>Enter Something Todo:</h2>
  <div class="container">

    <!-- This portion is for entering a To do -->
    <div class="form">
      <form action="/todo" method="post">
        <input id="nameInput" type="text" name="task" required>
        <br>
        <button type="submit">Add To Do</button>
      </form>
    </div>
    <!-- At this point, something needs to take this data and place it into an todoArray -->

    <section class="todo">
    <h1>To do:</h1>
      {{#todo}}{{^completed}}
      <form action="/complete/{{task}}" method="post">
        <button class="complete" type="checkbox" name="task" value="{{task}}">Mark as Complete</button>
      </form>
      <form action="/deleteindividual/{{task}}" method="post">
      <button type="submit">Delete</button>
      </form>
      <form action="/edit/{{task}}" method="post">
      <input type="text" name="task" placeholder="edit here + enter">
      </form> 
      <span>{{task}}</span>
      <br> {{/completed}}{{/todo}}
    </section>

    <section class="completed">
      <h1>Completed</h1>
      <form action="/deleteallcompleted" method="post">
      <button type="submit">Delete</button>
      </form>
      {{#todo}}{{#completed}}
      <p style="text-decoration: line-through">
        {{task}}
      </p>
    {{/completed}}{{/todo}}
    </section>
  </div>
</body>

</html>
<!-- This portion is for marking todos completed, this information is displayed from the array -->
<!-- and can be clicked upon to move it into the completed array  -->
<!-- <div class="todos">
    <h2>Get things done!</h2>
        {{#todo}}
        <p>{{name}}</p>
       <form action="/complete" method="post">
        <button type="submit" name="name" value="{{name}}">Mark Complete</button>
        </form>
        {{/todo}}
    </div>

This portion is for placing completed items back into the page
<div class="completed">
  <body style="text-align:center;">
  <h2>Completed Tasks</h2>
  <br>
  
  {{#markoff}}
  <p>{{name}}</p>
  {{/markoff}}
 
  <br>
  </div>
</body>

</html>
 -->